 // run both adaptive alpha and fixed alpha to generate the Graph similarity

process {
    withName: runBoth {  
        cpus = 2
        memory = '4 GB'
        time = '1h'
    }
}

docker.enabled = true  // enable the docker
